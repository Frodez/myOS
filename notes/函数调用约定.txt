由于函数调用时会遇到，入参顺序，入参位置，和谁来恢复调用堆栈的问题，故存在函数调用约定。

函数参数均由调用方进行入参操作。

--cdecl方式：
入参顺序和位置为从右至左从前往后，调用者恢复堆栈，也叫做手动清栈。

--stdcall方式：
入参顺序和位置为从右至左从前往后，被调用者恢复堆栈，也叫做被动清栈。

--thiscall方式：
入参顺序和位置为从右至左从前往后。
参数个数确定，则this指针通过ecx传递给被调用者，由被调用者恢复堆栈，类似stdcall。
参数个数不确定，则this指针在所有参数入栈后再入栈，由调用者恢复堆栈，类似cdecl。

--fastcall方式：
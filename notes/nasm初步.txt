nasm使用语法为intel汇编语法的变体。

nasm中$指当前行相对于当前段的地址，$$指当前段的起始地址。为段指定vstart=${vstart}可以主动修改当前段的起始地址，否则则由nasm编译器指定。
获得本段在文件中的真实地址，可以写section.${name}.start。

nasm中对于可以分为多部分的寄存器，为其指定数时，高位在左，低位在右。

vstart后面的等号不能有空格在两边，否则会无效且发出警告。